# Copy to .env.local and fill in values from your Supabase project
# (Project Settings -> API). If either is blank, the app falls back
# to in-repo fixtures in src/data/.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
